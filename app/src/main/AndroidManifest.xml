<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TennisSchedule"
        tools:targetApi="31">
        <activity
            android:name=".calendar.CalendarActivity"
            android:exported="true"
            android:label="Réservation">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
        </activity>
        <activity
            android:name=".logadmin.AdminActivity"
            android:exported="false"
            android:label="Page Administrateur" />
        <activity
            android:name=".logadmin.AddUserActivity"
            android:exported="false"
            android:label="Page d&apos;ajout d&apos;adhérent" />
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="Page de connexion">
        </activity>
    </application>

</manifest>